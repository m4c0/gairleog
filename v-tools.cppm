export module v:tools;
import hai;
import sv;
import vinyl;

#ifndef LECO_TARGET_WASM
import vee;
#endif

struct app_stuff;
struct ext_stuff;
using vv = vinyl::v<app_stuff, ext_stuff>;

namespace v {
}
